import { DynamicCanvasDirective } from './dynamic-canvas.directive';

describe('DynamicCanvasDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicCanvasDirective();
    expect(directive).toBeTruthy();
  });
});
